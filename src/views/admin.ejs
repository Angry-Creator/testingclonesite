<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Document</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;800&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        font-family: "Montserrat", sans-serif;
        padding: 0;
        margin: 0;
        box-sizing: content-box;
      }
      body {
        padding: 5px 10px;
      }
      .container {
        overflow: hidden;
      }

      header {
        margin: 5px 0;
        overflow: hidden;
      }
      header h1,
      h3 {
        text-align: center;
      }
      .user_block {
        margin: 10px 0;
        padding: 5px 0;
        border: 1px solid black;
        border-radius: 5px;
      }
      .user_block {
        padding: 10px 5px;
      }
      .user_block a {
        display: block;
        text-decoration: none;
        color: white;
        padding: 10px;
        background-color: blue;
        border-radius: 5px;
      }
      .user_block div {
        display: flex;
        flex-direction: row;
        word-wrap: wrap;
        word-break: break;
        padding: 10px;
      }
      .user_block div span {
        padding: 0 5px;
      }
      .user_block a {
        text-align: center;
      }
      @media (min-width: 992px) {
        .container {
          display: grid;
          grid-template-columns: 1fr 1fr;
          grid-column-gap: 10px;
        }
      }
      @media (min-width: 1020px) {
        .container {
          display: grid;
          grid-template-columns: 1fr 1fr 1fr;
          grid-column-gap: 10px;
        }
      }
    </style>
  </head>
  <body>
    <img
      src="/images/banner-rectangle-optimized.png"
      style="width: 100%; height: 80px"
    />
    <header>
      <h1>Admin Dashboard</h1>
      <h3>User Data's</h3>
      <div style="text-align: center; margin: 10px 0">
        <span
          >Total Clients: <%if(typeof(data) !== "undefined"){ %>
            <span><%=data.length%></span>
          <% } else { %> 
            <span>0</span>
          <% }%>
        </span>
        <span id="total"></span>
        <div style="margin: 10px">
          <a href="/logout">Logout</a>
        </div>
      </div>
    </header>
    <div class="container">
      <!--Auto generate user data-->
      <%if(typeof(data) !== "undefined"){ for(let i = 0; i < data.length; i++){
      %>
      <div class="user_block">
        <div>
          <span>First Name: </span><span><%= data[i].firstName %></span>
        </div>
        <div><span>Last Name: </span><span><%= data[i].lastName %></span></div>
        <div>
          <span>SSN: </span><span><%= data[i].socialSecurityNumber %></span>
        </div>
        <a href="/server/admin/user/<%=data[i].socialSecurityNumber%>"
          >Click to view more details</a
        >
      </div>
      <% } } %>
    </div>
  </body>
</html>
